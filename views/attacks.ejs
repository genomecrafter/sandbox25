<%- include('partials/header') %>

<style>
  /* Dark theme styling */
  body {
    background-color: #121212;
    color: #ffffff;
    margin: 0;
    font-family: 'Poppins', sans-serif;
  }

  /* Loader Styling */
  #loader {
    position: fixed;
    width: 100%;
    height: 100%;
    background: #121212;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000;
  }

  /* Animation on load */
  .animate-bottom {
    animation: fadeIn 1s ease-in-out;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Attack grid */
  .attack-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 20px;
    padding: 50px;
    max-width: 1200px;
    margin: auto;
  }

  /* Card Styling */
  .card {
    background-color: #1e1e1e;
    color: #ffffff;
    border-radius: 15px;
    box-shadow: 0px 5px 15px rgba(0, 255, 255, 0.2);
    padding: 20px;
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .card:hover {
    transform: translateY(-10px);
    box-shadow: 0px 8px 20px rgba(0, 255, 255, 0.4);
  }

  .card-title {
    font-size: 1.5rem;
    font-weight: bold;
    color: #00fff2;
    margin-bottom: 10px;
  }

  .card-text {
    font-size: 1rem;
    color: #e0e0e0;
  }

  .card-icon {
    font-size: 50px;
    color: #00fff2;
    margin-bottom: 15px;
  }
</style>

<body onload="myFunction()">
  <div id="loader">
    <img src="/images/loader.gif" alt="Loading..." width="100">
  </div>

  <div style="display:none;" id="myDiv" class="animate-bottom">
    <section id="attacks">
      <div class="attack-grid">
        
        <div class="card">
          <div class="card-icon">&#128274;</div>
          <h5 class="card-title">Denial of Service (DoS)</h5>
          <p class="card-text">A DoS attack floods a target system, making it inaccessible to legitimate users.</p>
        </div>

        <div class="card">
          <div class="card-icon">&#128269;</div>
          <h5 class="card-title">Probe</h5>
          <p class="card-text">Probing attacks scan network devices for vulnerabilities, aiming to exploit weaknesses.</p>
        </div>

        <div class="card">
          <div class="card-icon">&#128187;</div>
          <h5 class="card-title">Remote to User (R2L)</h5>
          <p class="card-text">R2L attacks allow hackers to send malicious packets to gain unauthorized access.</p>
        </div>

        <div class="card">
          <div class="card-icon">&#128736;</div>
          <h5 class="card-title">User to Root (U2R)</h5>
          <p class="card-text">U2R attacks start with limited access and gradually gain full administrative control.</p>
        </div>

        <div class="card">
          <div class="card-icon">&#128101;</div>
          <h5 class="card-title">Man-in-the-Middle (MitM)</h5>
          <p class="card-text">An attacker intercepts communication between two parties to steal or alter data.</p>
        </div>

        <div class="card">
          <div class="card-icon">&#128295;</div>
          <h5 class="card-title">SQL Injection</h5>
          <p class="card-text">SQL injection attacks manipulate database queries to access or modify sensitive data.</p>
        </div>
      </div>
    </section>

    <%- include('partials/footer') %>
  </div>

  <script>
    function myFunction() {
      setTimeout(() => {
        document.getElementById("loader").style.display = "none";
        document.getElementById("myDiv").style.display = "block";
      }, 300);
    }
  </script>
</body>
